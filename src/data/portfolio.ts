import type { PortfolioItem } from '../types'

/**
 * Case studies go here once real projects exist. Starts empty by design --
 * see PortfolioPage's empty state. No fabricated clients, logos or projects.
 *
 * Fields are not localised (client names, tech and image paths are facts, not
 * copy). If Welsh case-study copy is needed later, extend `title`/`summary`
 * to `{ en: string; cy: string }` at that point rather than now.
 */
export const portfolioItems: PortfolioItem[] = []
